---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: beskar7machinetemplates.
spec:
  group: ""
  names:
    kind: Beskar7MachineTemplate
    listKind: Beskar7MachineTemplateList
    plural: beskar7machinetemplates
    singular: beskar7machinetemplate
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            properties:
              template:
                properties:
                  spec:
                    properties:
                      configURL:
                        pattern: ^(https?|file)://.*\.(yaml|yml|json|toml|conf|cfg|ini|properties)$
                        type: string
                      imageURL:
                        pattern: ^(https?|ftp|file)://.*\.(iso|img|qcow2|vmdk|raw|vhd|vhdx|ova|ovf)(\.(gz|bz2|xz|zip|tar|tgz|tbz2|txz))?$
                        type: string
                      osFamily:
                        enum:
                        - kairos
                        - talos
                        - flatcar
                        - LeapMicro
                        - ubuntu
                        - rhel
                        - centos
                        - fedora
                        - debian
                        - opensuse
                        type: string
                      providerID:
                        type: string
                      provisioningMode:
                        default: RemoteConfig
                        enum:
                        - RemoteConfig
                        - PreBakedISO
                        - PXE
                        - iPXE
                        type: string
                    required:
                    - imageURL
                    - osFamily
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
