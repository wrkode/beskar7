---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: physicalhosts.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: PhysicalHost
    listKind: PhysicalHostList
    plural: physicalhosts
    singular: physicalhost
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Current state of the Physical Host
      jsonPath: .status.state
      name: State
      type: string
    - description: Indicates if the host is ready
      jsonPath: .status.ready
      name: Ready
      type: boolean
    - description: Creation timestamp
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PhysicalHost is the Schema for the physicalhosts API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object.'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents.'
            type: string
          metadata:
            type: object
          spec:
            description: PhysicalHostSpec defines the desired state of PhysicalHost
            properties:
              bootIsoSource:
                description: BootISOSource is the URL of the ISO image to use for provisioning
                type: string
              consumerRef:
                description: ConsumerRef is a reference to the Beskar7Machine that is using this host
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    type: string
                type: object
              redfishConnection:
                description: RedfishConnection contains the connection details for the Redfish endpoint
                properties:
                  address:
                    description: Address is the URL of the Redfish service
                    type: string
                  credentialsSecretRef:
                    description: CredentialsSecretRef is the name of the secret containing the Redfish credentials
                    type: string
                required:
                - address
                - credentialsSecretRef
                type: object
              userDataSecretRef:
                description: UserDataSecretRef is a reference to a secret containing cloud-init user data
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                  uid:
                    type: string
                type: object
            required:
            - redfishConnection
            type: object
          status:
            description: PhysicalHostStatus defines the observed state of PhysicalHost
            properties:
              conditions:
                description: Conditions contains the different condition statuses for this PhysicalHost
                items:
                  description: Condition defines an observation of a Cluster API resource operational state
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status to another
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about the transition
                      type: string
                    reason:
                      description: The reason for the condition's last transition in CamelCase
                      type: string
                    severity:
                      description: Severity provides an explicit classification of Reason code
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown
                      type: string
                    type:
                      description: Type of condition in CamelCase
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              errorMessage:
                description: ErrorMessage contains details on the last error encountered
                type: string
              hardwareDetails:
                description: HardwareDetails contains information about the physical host hardware
                properties:
                  firmware:
                    properties:
                      version:
                        type: string
                    type: object
                  manufacturer:
                    type: string
                  model:
                    type: string
                  serialNumber:
                    type: string
                type: object
              observedPowerState:
                description: ObservedPowerState is the last observed power state from Redfish endpoint
                type: string
              ready:
                description: Ready indicates if the host is ready and enrolled
                type: boolean
              state:
                description: State represents the current state of the host
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {} 