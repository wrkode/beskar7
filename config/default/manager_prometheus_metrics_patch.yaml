apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: beskar7-system
spec:
  template:
    spec:
      containers:
      - name: manager
        ports:
        - containerPort: 8080
          protocol: TCP
          name: metrics 